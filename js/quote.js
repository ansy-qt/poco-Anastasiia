let quote = prompt("Please enter a quote:");
let author = prompt("Who is the author of the quote?");
alert(`${author} says, "${quote}"`);



function displayQuote() {
    let quote = prompt("Please enter a quote:");
    let author = prompt("Who is the author of the quote?");
    alert(`${author} says, "${quote}"`);
  }
  
  displayQuote();
  